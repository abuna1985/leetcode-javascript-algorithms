const functionName = require('./path/to/file');

describe('Edge Cases', () => {
	test('It should ____', () => {
		expect(() =>
			functionName(argument).toThrowError(new Error('Error message'))
		);
	});

	test('It should ____', () => {
		expect(() =>
			functionName(argument).toThrowError(new Error('Error message'))
		);
	});
});

describe('Test Cases', () => {
	// Test Cases
	test('It should return result when running functionName(argument)', () => {
		expect(functionName(argument)).toStrictEqual(result);
	});

	test('It should return result when running functionName(argument)', () => {
		expect(functionName(argument)).toStrictEqual(result);
	});

	test('It should return result when running functionName(argument)', () => {
		expect(functionName(argument)).toStrictEqual(result);
	});

	test('It should return result when running functionName(argument)', () => {
		expect(functionName(argument)).toStrictEqual(result);
	});
	
	test('It should return result when running functionName(argument)', () => {
		expect(functionName(argument)).toStrictEqual(result);
	});
});
